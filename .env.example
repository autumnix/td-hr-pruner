# Paste the full Cookie header from a logged-in TorrentDay tab
# (DevTools → Application → Cookies → copy as "name=value; name=value; …")
TD_COOKIES=

# Optional Pushover notifications
PUSHOVER_TOKEN=
PUSHOVER_USER=
